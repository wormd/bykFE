import {BeautifyNumPipe} from './beautify-num.pipe';

describe('BeautifyNumPipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifyNumPipe();
    expect(pipe).toBeTruthy();
  });
});
